# Contraseña del panel de administración (#admin).
# ⚠️ Esto es solo un "gate" del lado del cliente: la variable acaba en el
# bundle público. NO es seguridad real. Para datos privados de verdad,
# mover los precios a un backend con autenticación del lado del servidor.
# Si no se define, se usa un valor por defecto en el código.
VITE_ADMIN_PASSWORD=eurekasi2026
